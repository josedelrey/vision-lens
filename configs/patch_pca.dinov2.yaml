input:
  files:
    - examples/5.jpg
    - examples/6.jpg

model:
  architecture: vit
  backend: timm
  name: hf_hub:timm/vit_base_patch14_dinov2.lvd142m

preprocessing:
  interpolation: bilinear

analysis:
  method: patch_pca
  foreground_side: low

visualization:
  output_size: match
  interpolation: bilinear_mask

output:
  directory: outputs/image/dinov2_patch_pca
  overwrite: replace
